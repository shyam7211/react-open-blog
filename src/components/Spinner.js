import Book from './Book.gif'

const Spinner = () => {
    return ( 
        <div style={{textAlign:'center'}}>
           <img src={Book} alt="loading..." /> 
        </div>
     );
}
 
export default Spinner;